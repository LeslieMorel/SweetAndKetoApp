import { HorarioEntregaPipePipe } from './horario-entrega-pipe.pipe';

describe('HorarioEntregaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HorarioEntregaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
