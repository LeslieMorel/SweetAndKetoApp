import { EstadoOrdenPipePipe } from './estado-orden-pipe.pipe';

describe('EstadoOrdenPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoOrdenPipePipe();
    expect(pipe).toBeTruthy();
  });
});
